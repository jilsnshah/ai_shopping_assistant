# Environment Configuration for AI Shopping Assistant

# ==================== GOOGLE CLOUD SQL (PostgreSQL) ====================
# LangGraph conversation memory persistence using Google Cloud SQL
# 
# Setup Instructions:
# 1. Create Cloud SQL instance: 
#    gcloud sql instances create langgraph-db --database-version=POSTGRES_15 --tier=db-f1-micro --region=us-central1
#
# 2. Create database and user:
#    gcloud sql databases create langgraph --instance=langgraph-db
#    gcloud sql users create langgraph_user --instance=langgraph-db --password='your_secure_password'
#
# 3. Authorize your IP:
#    gcloud sql instances patch langgraph-db --authorized-networks=YOUR_PUBLIC_IP/32
#
# 4. Get your Cloud SQL IP:
#    gcloud sql instances describe langgraph-db --format='value(ipAddresses[0].ipAddress)'

# PostgreSQL Connection String
# Format: postgresql://username:password@host:port/database
#
# For Google Cloud SQL Public IP:
LANGGRAPH_DB_URI=postgresql://langgraph_user:your_secure_password@34.71.XX.XX:5432/langgraph
#
# For Google Cloud SQL Private IP (from Cloud Run/App Engine):
# LANGGRAPH_DB_URI=postgresql://langgraph_user:your_secure_password@10.123.0.2/langgraph?sslmode=disable
#
# For local PostgreSQL development:
# LANGGRAPH_DB_URI=postgresql://postgres:postgres@localhost:5432/langgraph


# ==================== FIREBASE REALTIME DATABASE ====================
# Firebase is used for storing buyer/seller data (not conversation memory)
# Credentials file: firebase-credentials.json (already configured)

FIREBASE_DATABASE_URL=https://ai-shopping-assistant-jils-default-rtdb.firebaseio.com/


# ==================== GEMINI AI API ====================
# Get your API key from: https://aistudio.google.com/app/apikey

GEMINI_API_KEY=your_gemini_api_key_here


# ==================== WHATSAPP BUSINESS API ====================
# WhatsApp Business API credentials for sending messages

WHATSAPP_API_URL=https://graph.facebook.com/v17.0/YOUR_PHONE_NUMBER_ID/messages
WHATSAPP_ACCESS_TOKEN=your_whatsapp_access_token_here


# ==================== APPLICATION SETTINGS ====================

# Flask environment (development or production)
FLASK_ENV=development

# Flask debug mode (true or false)
FLASK_DEBUG=true

# Application host and port
FLASK_HOST=0.0.0.0
FLASK_PORT=5000


# ==================== GOOGLE CLOUD SQL PRICING ====================
# db-f1-micro (Shared CPU, 0.6GB RAM):
#   - Always Free Tier: $0/month (1 instance)
#   - After free tier: ~$5/month
#
# db-n1-standard-1 (1 vCPU, 3.75GB RAM):
#   - ~$50/month (high traffic production)
#
# Storage: 10GB included, additional storage ~$0.17/GB/month
